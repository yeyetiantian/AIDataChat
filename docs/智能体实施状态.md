# 智能体实施状态

> 按《智能体能力差距与行动计划》中的优先级执行。每完成一个可验证单元即更新本文件。

| 时间 | 优先级 | 行动项 | 状态 | 说明 |
| --- | --- | --- | --- | --- |
| 2026-07-13 | P0 / A1 | 规则配置推荐领域契约 | 已完成 | 新增 `RuleConfigRecommendation`、逻辑树、函数调用、信号、参数、证据和校验问题模型。 |
| 2026-07-13 | P0 / A2 | 规则知识检索服务 | 已完成 | 新增任务、规则、函数、信号/报警检索上下文；Agent 只能基于检索事实生成方案。 |
| 2026-07-13 | P0 / A3 | 规则-信号关系与报警统计可用性 | 已完成（基础） | 新增 `rule_signals`、兼容 `signal_stats` 迁移和状态字段；仍待接入外部实时/周期统计同步。 |
| 2026-07-13 | P0 / A4 | 规则配置校验器 | 已完成 | 校验任务、函数、信号、逻辑树和判断条件；错误不会被伪装成可发布规则。 |
| 2026-07-13 | P0 / A10 | 规则推荐前端展示与会话持久化 | 已完成 | Chat 响应、SQLite `rules_json`、会话恢复和规则建议卡片均已接入。 |
| 2026-07-13 | P0 / A5 | 结构化看板草案 | 已完成（基础） | 问卷现提交任务/规则/信号 ID 与图表槽 JSON；后端持久化 `agent_drafts`，Chart Agent 将其作为图表数量和内容约束。 |
| 2026-07-13 | P0 / A6 | 看板模板 | 已完成（首批） | 新增“报警总览”和“任务规则诊断”模板；按图表职责、共享约束和 1–6 图上限生成方案并注入 Chart Agent。 |
| 2026-07-13 | P1 / A7 | 指标字典与图表分析计划 | 已完成（基础） | 新增报警记录数、持续时长、信号均值等口径，并要求 Agent 在每张图上说明目标、口径、原因和假设。 |
| 2026-07-13 | P1 / A8 | 执行后数据画像与图形复核 | 已完成（基础） | 依据时间轴和类别基数复核图形；明显不适配时自动切换为折线/柱状，并随图表返回原因和数据画像。 |
| 2026-07-13 | P1 / A9 | 执行失败定向修复 | 已完成 | 全部图表失败时，将已脱敏错误反馈给 Agent 重建一次；限制一次，避免循环。 |
| 2026-07-13 | P2 | 异步运行时、评测集和反馈闭环 | 未开始 | 作为下一阶段，需先确认真实 Pivot API 的并发/限流能力。 |

## 当前验证

- Python 静态编译：通过。
- SQLite 初始化与历史 `signal_stats` 迁移：通过。
- 已验证规则知识检索和规则校验能识别不存在的函数、信号。
- 已验证结构化看板草案可保存、读取并注入图表推荐提示词。
- 已通过替身结构化 LLM 验证 Rule Agent 会将检索到的函数、信号写入草案，并通过确定性校验。
- 前端生产构建：通过。过程中修复了 `TraceSpanNode.vue` 从 Vue SFC 导入未导出类型导致的 TypeScript 阻断。

## 已知限制

- 现有 `signal_stats` 的报警统计周期和外部刷新机制尚未定义；系统会明确返回 `no_data`/`unavailable`，不会把它误认为“报警为零”。
- 规则建议目前仅生成草案，不会自动发布到外部规则系统。
